import wollok.game.*
import personaje.*
import cultivos.*

program farmville {
	//  CONFIG	
	game.title("La granja")
	game.height(10)
	game.width(10)
	game.ground("ground.png")

	//  VISUALES
 	game.addVisualCharacter(personaje)
	//game.addVisual(Trigo)
	//game.addVisual(Tomaco)
	//game.addVisual(Maiz)
 	
	//EVENTOS
	keyboard.m().onPressDo({new Maiz().plantar()})
	keyboard.t().onPressDo({new Trigo().plantar()})
	keyboard.o().onPressDo({new Tomaco().plantar()})
 	//  COMIENZA
	game.start()
}
